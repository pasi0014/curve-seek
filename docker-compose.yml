services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - DB_PATH=/data/road-conditions.sqlite
    volumes:
      - app-data:/data
    restart: unless-stopped

volumes:
  app-data:
